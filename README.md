# img-expander
Expand an image to some other container or coords

    expander(imgEl, containerEl);
    expander(imgEl, {
      top: 100,
      left: 100,
      width: 800,
      height: 400
    });


